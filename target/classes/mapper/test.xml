<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test.select">
	<select id="selectMember" resultType="test.TestVO" parameterType="test.TestVO">
		select * from spring5fs.member
	</select>
	
	<insert id="insertMember" parameterType="test.TestVO">
		INSERT INTO spring5fs.member (email, name, password, regdate, filename, info) values
		(#{email}, #{name}, #{password}, now(), #{filename}, #{info})
	</insert>
</mapper>

